# QueryGraph TypeScript releases

The TypeScript package follows the unified QueryGraph repository and preserves
the same semantic and security wire contracts as the Rust and Python APIs.

| Version | Notes |
| --- | --- |
| 0.1.1 | Compatibility patch exporting the Python-equivalent `CRYPTO_AVAILABLE` capability probe. |
| 0.1.0 | Initial public TypeScript API and npm package. |
