import { CroissantDataset } from "./croissant.js";
import { CdifResource } from "./cdif.js";
import { OsiDocument } from "./osi.js";
export interface NavigatorInput { datasetName: string; description: string; landingPage: string; dataUrl: string; creator?: string; agentName?: string }
export interface NavigatorOutput { croissant: Record<string, unknown>; cdif: Record<string, unknown>; did: Record<string, unknown>; odrl: Record<string, unknown>; osi: OsiDocument }
export class AiNavigator { build(input: NavigatorInput): NavigatorOutput { const id = `${input.landingPage.replace(/\/$/, "")}/dataset/${encodeURIComponent(input.datasetName.toLowerCase().replace(/\s+/g, "-"))}`; const dataset = new CroissantDataset({ id, name: input.datasetName, description: input.description, license: "https://creativecommons.org/publicdomain/zero/1.0/", creators: [input.creator ?? "QueryGraph"], files: [{ id: `${id}/file`, name: `${input.datasetName}.csv`, contentUrl: input.dataUrl, encodingFormat: "text/csv" } as never], recordSets: [], keywords: input.datasetName.split(/\s+/) }); const cdif = CdifResource.fromCroissant(dataset, input.landingPage, "https://querygraph.ai/v1"); const osi = OsiDocument.fromCroissant(dataset); return { croissant: dataset.toJsonLd(), cdif: cdif.toJsonLd(), did: { id: `did:oyd:${encodeURIComponent(id)}` }, odrl: { "@type": "Policy", permission: [] }, osi: osi }; } }
