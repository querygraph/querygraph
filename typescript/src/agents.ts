import type { TypeDidAgent } from "./typedid.js";
export function toolDescription(agent: TypeDidAgent): string { return `Governed QueryGraph agent ${agent.name}`; }
export function toToolSchema(agent: TypeDidAgent, flavor: "openai" | "anthropic" = "openai"): Record<string, unknown> { const functionShape = { name: agent.name.toLowerCase().replace(/[^a-z0-9_]+/g, "_"), description: toolDescription(agent), input_schema: { type: "object", properties: { question: { type: "string", description: "Governed question" } }, required: ["question"] } }; return flavor === "anthropic" ? functionShape : { type: "function", function: { name: functionShape.name, description: functionShape.description, parameters: functionShape.input_schema } }; }
export interface TypeDidAgentRun { agent: string; status: "allowed" | "denied"; summary: string; evidence: string[]; redactions: string[] }
export function deterministicSpecialist(agent: TypeDidAgent, question: string): TypeDidAgentRun { return { agent: agent.name, status: "allowed", summary: `Deterministic answer for: ${question}`, evidence: [], redactions: [] }; }
export class TypeDidLangChainToolAdapter { constructor(public readonly agent: TypeDidAgent, private readonly handler: (input: Record<string, unknown>) => unknown) {} asTool(): { name: string; description: string; invoke: (input: Record<string, unknown>) => unknown } { return { name: this.agent.name, description: toolDescription(this.agent), invoke: this.handler }; } async invoke(input: Record<string, unknown>): Promise<unknown> { return this.handler(input); } }
